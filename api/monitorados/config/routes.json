{
  "routes": [
    {
      "method": "GET",
      "path": "/monitorados",
      "handler": "monitorados.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/monitorados/count",
      "handler": "monitorados.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/monitorados/:id",
      "handler": "monitorados.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/monitorados",
      "handler": "monitorados.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/monitorados/:id",
      "handler": "monitorados.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/monitorados/:id",
      "handler": "monitorados.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
